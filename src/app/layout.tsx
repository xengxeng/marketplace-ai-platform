import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Marketplace-AI",
  description: "Enterprise marketplace platform with merchant and reseller commerce",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-screen bg-[radial-gradient(circle_at_top,_rgba(255,90,90,0.16),_transparent_42%),linear-gradient(135deg,_#060606,_#111111)] text-zinc-100">
        {children}
      </body>
    </html>
  );
}
